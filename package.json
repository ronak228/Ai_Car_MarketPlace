{
  "name": "car-price-predictor-mern",
  "version": "1.0.0",
  "description": "Advanced Car Price Predictor with Market Trends Analysis",
  "main": "unified_app.py",
  "scripts": {
    "start": "python unified_app.py",
    "dev": "python unified_app.py",
    "client": "cd client && npm start",
    "server": "python unified_app.py",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "install-server": "pip install -r requirements.txt",
    "install-all": "npm run install-server && npm run install-client",
    "setup": "npm run install-all && cd client && echo REACT_APP_USE_LOCAL_AUTH=true > .env",
    "run-all": "concurrently \"npm run server\" \"npm run client\"",
    "test": "cd client && npm test",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "keywords": [
    "car-price-prediction",
    "machine-learning",
    "react",
    "flask",
    "market-trends",
    "analytics"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0",
    "python": ">=3.8.0"
  },
  "repository": {
    "type": "git",
    "url": "your-repo-url"
  }
}
